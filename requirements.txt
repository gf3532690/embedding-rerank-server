fastapi==0.115.0
uvicorn[standard]==0.30.6
pyyaml==6.0.2
pydantic==2.9.2
# numpy 必须锁定 <2.0：onnxruntime 1.19.2 / FlagEmbedding 1.2.11 等老栈
# 部分 wheel 针对 numpy 1.x ABI 编译，装 numpy 2.x 会运行时崩溃
numpy>=1.24.0,<2.0.0

# === ML 核心依赖（先锁定版本，再装 FlagEmbedding 避免被覆盖）===
transformers==4.44.2
sentence-transformers==3.1.1
accelerate==0.33.0
peft>=0.11.0
datasets>=2.19.0
sentencepiece>=0.1.99
protobuf>=3.20.0

# FlagEmbedding 放最后，它的依赖已被上面锁定
FlagEmbedding==1.2.11

# Flash Attention（GPU 长文本加速，可选 — 需要 Ampere+ GPU）
# 安装后 transformers 自动启用，无需代码改动
# 如果安装失败（无 GPU 或架构不支持），不影响服务运行
# pip install flash-attn --no-build-isolation
flash-attn>=2.5.0; platform_machine == "x86_64"

# Prometheus 监控
prometheus_client>=0.20.0

# 性能优化（可选）
uvloop>=0.19.0; sys_platform != "win32"
orjson>=3.9.0

# torch 由基础镜像 pytorch/pytorch:2.6.0-cuda12.4-cudnn9-runtime 提供
